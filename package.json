{
  "private": true,
  "engines": {
    "node": ">=8.0.0 <9"
  },
  "scripts": {
    "lint": "standard 'server/**/*.js' --verbose | snazzy",
    "start": "yarn lint && rollup -c && node build/server.js",
    "start-w": "nodemon --watch server --exec 'yarn start'"
  },
  "dependencies": {
    "basic-auth": "^1.1.0",
    "dotenv": "^4.0.0",
    "koa": "^2.3.0",
    "koa-route": "^3.2.0",
    "koa-static": "^4.0.1",
    "md5": "^2.2.1",
    "rollup": "^0.49.2",
    "snazzy": "^7.0.0",
    "standard": "^10.0.3",
    "stylus": "^0.54.5"
  },
  "devDependencies": {
    "nodemon": "^1.11.0"
  }
}
