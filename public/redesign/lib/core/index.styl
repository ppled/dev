@require './reset.styl'
@require './typography.styl'
@require './grid.styl'
@require '../utils/vars.styl'

.constrain {
  max-width: 1140px
  margin: 0 auto
  padding: 0 15px
}

.display-block {
  display: block
}
