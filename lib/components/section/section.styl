@import '../../_core/mixins.styl'
@import '../../_core/vars.styl'

.section {

  +media('>=' + BREAKPOINTS.mobile) {
    padding-bottom: 65px
    padding-top: @padding-bottom
  }

  +media('<' + BREAKPOINTS.mobile) {
    padding-bottom: 40px
    padding-top: @padding-bottom
  }

  .section-heading {
    font-size: 25px
    margin-bottom: 30px
  }
}

.section[dark-bg] {
  color: white
}
